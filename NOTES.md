# Notes

- **Explain one fix.** **Frontent Bug Fix**- So the Issue for this bug is that there's no event listener for the **End Date Input** so the result is no update in total if there are changes in End Date Input, so I added an Event listener for the End Date Input, the result is it updates the total when there are changes in End Date Input.
- **Show the failure for double-booking bug.** So the bug just checks the Start Date Input, For example there is an existing booking date of January 10 to 15, when the user book a date from January 5 to 11 the book will be saved, but if the user book a date from January 14 to 16 the book will not be saved, so my fix is to add a condition for the end date, similar to the condition on the start date, as for other conditions, I can explain it on the next step of the hiring process.
- **AI use.** So I didn't use AI for this because I had so much fun fixing the bugs, but if I use AI and checking if its response is correct, I'll check the documentation, and check also the output if it's align on the documentation, and also check the internet if the AI code meets the industry standards.
